# not set - autodetect
# HOST_OS="freebsd"
HOST_CHROOT_DIR="/var/chroots/xorg-testing"
GIT_CLONE_ARGS="--depth 1"
XORG_PREFIX="/usr/local/X11"

# target when running on FreeBSD host
FREEBSD_TARGET_ID="freebsd-14"

# target when running on Debian host
DEBIAN_TARGET_ID="debian-bookworm"

# target when running on NetBSD host
NETBSD_TARGET_ID="netbsd-10"

# target when running on OpenBSD host
OPENBSD_TARGET_ID="openbsd-7.5"
